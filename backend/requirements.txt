# =============================================================================
# SINALibras backend — runtime dependencies
# Pinned for reproducibility; bump deliberately, not opportunistically.
# =============================================================================

# Web framework + ASGI server
fastapi==0.110.1
uvicorn[standard]==0.25.0

# Data validation / settings
pydantic>=2.6.4,<3
pydantic-settings>=2.2.1,<3
email-validator>=2.2.0

# Database
motor==3.3.1
pymongo==4.5.0

# Auth.
# - PyJWT (not python-jose: CVE + unmaintained).
# - bcrypt directly (not passlib: passlib<1.7.5 emits a noisy warning against
#   bcrypt>=4.1).
pyjwt>=2.10.1
bcrypt>=4.1.3,<5

# Rate limiting (used by /auth/* routes)
slowapi>=0.1.9

# Observability
structlog>=24.1.0

# Misc utilities (kept from the template; review in Sprint 2)
python-dotenv>=1.0.1
python-multipart>=0.0.9
tzdata>=2024.2

# =============================================================================
# Dev / test (move to requirements-dev.txt when CI lands in Sprint 6)
# =============================================================================
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
