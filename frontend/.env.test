# Loaded automatically by react-scripts when running `yarn test`.
# The actual URL doesn't matter — axios calls are mocked in tests — but
# `src/lib/env.js` throws on an undefined REACT_APP_BACKEND_URL, so we
# pin one here to keep imports clean.
REACT_APP_BACKEND_URL=http://localhost:8000
